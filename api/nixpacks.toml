[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm-9_x']

[phases.install]
cmds = ['npm ci --only=production']

[phases.build]
cmds = ['npm run build || true']

[phases.start]
cmd = 'npm start'

[providers]
node = '18'
